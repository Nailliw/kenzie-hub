import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles(() => ({
	usersNavBar: {
		width: "100%",
		margin: "1rem auto",
		padding: "1rem",
		textAlign: "center",
	},
}));
